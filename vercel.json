{
    "builds": [{
        "src": "calldata/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "calldata/wsgi.py"
        }
    ]
}
